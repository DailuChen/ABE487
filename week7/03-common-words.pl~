#!/usr/bin/env perl

use strict;
use warnings;
use autodie;
use feature 'say';

my ($file1, $file2) = @ARGV;
open my $fh1, '<', $file1;
open my $fh2, '<', $file2;

$word =~ s/[^A-Za-z0-9]//g;
while (my $line1 = <$fh1>, my $line2 = <$fh2>){
    chomp $line1, $line2;

